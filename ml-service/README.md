# ml-service

Python FastAPI microservice for student failure-risk prediction.

Models: Logistic Regression (baseline), Random Forest (benchmark), XGBoost (primary).

Input features: attendance_rate, cat_score_avg, assignment_submission_rate, late_submission_rate, fee_balance_outstanding, units_registered, year_of_study, prior_unit_failure. Target: result.

_Placeholder — no code yet._
